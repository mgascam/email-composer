import React from 'react';
import Section from './Section';
const NotFound = () => (
    <div>
        <Section>
            <h1>Not found</h1>
        </Section>
    </div>
);

export default NotFound;
